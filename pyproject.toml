[project]
name = "proyecto-clase"
version = "0.1.0"
requires-python = ">=3.14.5"

dependencies = [
    "asyncpg>=0.31.0",
    "fastapi[standard]>=0.141.1",
    "loguru>=0.7.3",
]


[tool.fastapi]
entrypoint = "main:app"